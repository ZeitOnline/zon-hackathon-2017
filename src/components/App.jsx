import React from 'react';

import { Header, TeaserList, AudioSettings, Player } from 'app/components';

const App = () => (
    <div>
        <Header />
        <AudioSettings />
        <TeaserList />
        <Player />
    </div>
);

export default App;
